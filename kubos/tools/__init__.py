"""The tools in this module return geometric objects for a given input point.
If no valid geometric object can be constructed, "None" is returned."""

# For finding out if an object is valid, error handling is used where OCC will
# throw errors and other validation mechanisms are used otherwise
